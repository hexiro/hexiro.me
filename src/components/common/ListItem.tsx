import { styled } from "theme";

const ListItem = styled("li", {
    color: "$text-primary",
    fontFamily: "$heading",
    fontSize: 28,
    fontWeight: 500,
    listStyle: "none",
});

export default ListItem;
